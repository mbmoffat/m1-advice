// LOCKED — human-only edits, followed by `npm run lock`.
// Swap to hashed token after FormSubmit activation, guard-enforced.
// (prebuild guard accepts the plain email endpoint or a hashed token of 16+ hex chars)
export const FORM_ENDPOINT = 'https://formsubmit.co/enquiry@mortgageonefinance.co.uk';
