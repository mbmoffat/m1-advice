Build the next 2 to 4 pages with status planned from docs/roadmap.md, in the CLAUDE.md build order. The target keyword is already in each row, do not run Ahrefs. One PR per page, full per-page protocol from CLAUDE.md, set each built row to built and commit, npm run build must pass, PRs auto-merge on green. If the current cluster is done and the next is not stocked, build nothing and say so. Report each page and its PR.
